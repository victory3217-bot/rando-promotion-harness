# 홍보 콘텐츠 제작 Harness — 최상위 실행 지침

이 저장소는 소규모 사업자의 홍보문구·예약안내·상품소개·이미지 제작을 실제로 수행하는 작업공간이다.
강의/교육용이 아니라 **실제 게시·발송에 쓰이는 콘텐츠**를 만드는 것이 목적이다.

## 정보 우선순위

요청을 처리할 때 아래 순서로 정보를 신뢰한다. 충돌하면 더 최근에 확인된 사업 사실을 우선하고,
그래도 불확실하면 사용자에게 질문하거나 `[확인필요]`로 남긴다.

1. `inputs/business-info-card.md` — 이번 작업에서 사용하는 상품·서비스의 사실 SSOT (가격·일정·환불·상품조건의 유일한 기준)
2. `skills/*/SKILL.md` — 요청 채널/문서 유형의 작성 형식과 표현 규칙
3. `knowledge/personal-profile.md` — 운영자(사용자)의 말투와 답변 선호
4. 대화 중 사용자가 새로 제공한 최신 정보

## 정보 수집 원칙 (점진적 입력)

`inputs/business-info-card.md`는 26개 항목을 한 번에 다 받는 고정 Form이 아니다. 아래 원칙으로
필요한 만큼만 점진적으로 수집한다.

1. 처음부터 모든 항목을 요구하지 않는다. 카드의 "핵심 입력"(최대 6개)만 우선 확인한다.
2. 나머지("추가 입력") 항목은 사용자가 요청한 콘텐츠 유형을 판단한 뒤, 그 콘텐츠 제작에 실제로
   필요한 경우에만 질문한다.
3. 사용자가 이미 대화 중에 제공했거나 카드에 이미 채워진 정보는 다시 질문하지 않는다.
4. 결과물 작성에 필수적이지 않은 정보는 질문하지 않고 생략한다.
5. 가격·일정·예약방법·환불조건 등 사실정보가 결과물에 반드시 필요한데 아직 확인되지 않았을 때만
   추가로 질문한다.
6. 여러 항목을 질문해야 하면 한 번에 묶어서 질문한다.
7. 질문했는데 사용자가 모른다고 답하면, 결과물에 꼭 필요한 항목일 때만 `[확인필요]`로
   `inputs/business-info-card.md`에 기록한다. 필요 없는 항목은 묻지 않고 비워 둔다.

## 절대 규칙 (Governance)

- `inputs/business-info-card.md`에 없는 가격·일정·환불·상품조건을 만들지 않는다. 없으면 `[확인필요]`.
- 고객 이름·전화번호·주소·예약내역 등 개인정보를 요구하거나 프롬프트에 넣지 않는다.
- 과장·최상급·근거 없는 효능 표현을 쓰지 않는다 (예: "특별한 경험", "잊지 못할 추억", "최고", "유일한", "힐링").
- 채널마다 목적을 구분한다.
  - SNS(블로그/페이스북/인스타그램): 아직 모르는 고객의 관심과 문의 유도
  - 상품소개: 구매를 고민하는 고객의 판단 지원
  - 예약안내: 이미 예약한 고객의 방문 준비 지원
- 긴 원본을 먼저 만든 뒤 채널에 맞게 재가공한다. 채널별로 처음부터 따로 쓰지 않는다.
- 이미지 생성 시 실제 사진이 있으면 실제 사진을 우선하고, AI 이미지는 보조·초안으로만 쓴다.
- 사업정보카드나 개인 프로필에 없는 항목을 임의로 채우지 않는다.
- 외부 고객에게 보여지는 **최종 게시 문안에는 `[확인필요]`·미확정 상태를 기본적으로 노출하지 않는다.**
  가격·일정·운영시간·예약방법·환불조건 등이 `[확인필요]`이거나 미정이면:
  1. 그 정보 없이도 문안 작성이 가능하면 최종 문안에서는 해당 내용을 생략한다.
  2. 내부적으로 관리하는 `[확인필요]` 항목 목록에는 그대로 남긴다.
  3. 그 정보가 콘텐츠 작성에 반드시 필요하면(생략하면 문안이 성립하지 않으면) 사용자에게 질문한다.
  4. 사용자가 명시적으로 "미정이라고 표시해라", "추후 공지라고 써라"처럼 요청한 경우에만
     최종 문안에 미확정 상태를 표시한다.

## 채널 공통 사실검증 절차 (모든 결과물에 항상 적용)

어떤 채널·문서 유형이든 결과물을 낼 때는 **항상** 아래 순서로 마무리한다. 이 절차는 특정 Skill에
속하지 않고 이 파일을 통해 항상 적용된다.

1. 결과물 안의 가격·일정·환불·상품조건·연락처 등 사실 항목을 `inputs/business-info-card.md`와 하나씩 대조한다.
2. 카드에 없거나 `[확인필요]`·미확정인 항목이 최종 문안에 그대로 노출되지 않았는지 확인한다.
   위 Governance 규칙에 따라 생략되었거나, 사용자가 명시적으로 미확정 표시를 요청한 경우만 예외로
   허용한다. 확인되지 않은 항목은 최종 문안이 아니라 `[확인필요]` 항목 목록에 남긴다.
3. 고객 개인정보가 포함되지 않았는지 확인한다.
4. 과장·최상급·근거 없는 효능 표현이 없는지 확인한다.
5. 결과물 마지막에 다음 형식을 항상 붙인다.
   1. 최종 문안
   2. `[확인필요]` 항목 목록
   3. 사실확인 체크 (위 1~4 항목을 통과했는지)
   4. 다음 수정 제안 1~3개

## 작업 흐름

1. `inputs/business-info-card.md`의 핵심 입력과 `knowledge/personal-profile.md`를 읽는다.
   핵심 입력이 비어 있으면 먼저 채우도록 질문한다.
2. 요청된 채널/문서 유형을 판단하고 맞는 `skills/*/SKILL.md`를 정한다
   (`blog-promotion`, `facebook-promotion`, `instagram-promotion`, `reservation-message`, `product-description`).
3. 위 "정보 수집 원칙"에 따라 그 콘텐츠 제작에 실제로 필요한 추가 입력만 판단해, 카드에 비어 있고
   대화 중에도 아직 없는 항목만 한 번에 묶어서 질문한다.
4. 답변을 `inputs/business-info-card.md`의 해당 항목에 기록한다.
5. 초안을 만든다.
6. 위 "채널 공통 사실검증 절차"를 적용한다.
7. 최종 결과물 + 확인필요 항목 + 사실확인 체크 + 다음 제안을 함께 제시한다.

콘텐츠 제작 요청의 실행 주체는 `agents/promotion-agent.md`에 정의되어 있다.

## 참고

- 원본 교육자료(4시간 실습 키트) 13개는 `legacy/`에 원문 그대로 보존되어 있다. 이 폴더는 참고용이며
  실제 작업 흐름에서는 사용하지 않는다.
- SSOT 최초 작성 시 `legacy/12_Google_Form_Setup_and_Email_Return.md`의 Google Form 방식을 참고할 수 있다.
